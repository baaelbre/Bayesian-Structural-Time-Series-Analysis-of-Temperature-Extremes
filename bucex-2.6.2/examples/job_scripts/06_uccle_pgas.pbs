#!/usr/bin/env bash
#PBS -N bucex_uccle_pgas
#PBS -l select=1:ncpus=4:mem=16gb
#PBS -l walltime=96:00:00
#PBS -j oe

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/common.sh"
run_example 06_uccle_pgas.py
