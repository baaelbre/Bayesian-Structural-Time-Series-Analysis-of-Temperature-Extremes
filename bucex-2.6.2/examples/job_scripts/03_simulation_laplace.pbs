#!/usr/bin/env bash
#PBS -N bucex_sim_laplace
#PBS -l select=1:ncpus=4:mem=12gb
#PBS -l walltime=24:00:00
#PBS -j oe

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/common.sh"
run_example 03_simulation_laplace.py
