[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bucex"
version = "0.1.0"
description = "Bayesian Unobserved Components for Extremes"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Bas"}]
dependencies = [
  "numpy",
  "scipy",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["bucex*"]
