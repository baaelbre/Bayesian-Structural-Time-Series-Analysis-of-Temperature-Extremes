# things that need to be solved in the next update
Guided disturbance PGAS has a reproducible singular-support bug.
Only rerun the expensive mixed PGAS model after the guided-PGAS hotfix and initial-slope correction.
# results of the prior sensitivity experiment
best computational behaviour and reasonable recovery of the two nonzero innovations. It cannot select an innovation variance exactly equal to zero, so it invents some seasonal dynamics

# initial slope
of course the initial slope and level should be estimated

# common factor model
idiosyncratic shocks ... hmm not ideal, the triple gamma is overparametrized
Rerun the Gaussian factor with normal and PC priors to establish that the decomposition can be recovered without aggressive shrinkage.