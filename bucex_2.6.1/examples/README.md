# bucex 2.6.1 examples

The example surface is intentionally limited to the COMPSTAT analysis. Run
the numbered scripts in [`presentation/`](presentation/) in order. The same
steps are available as PBS jobs in [`job_scripts/`](job_scripts/).
