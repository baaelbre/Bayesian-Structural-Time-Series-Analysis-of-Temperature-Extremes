laplace is not yet available for the multi series models? Hierarchical mixed/GEV SSVS

# speed up
vectorizing the PGAS (especially for the mixed model with SSVS), for wall time reduction
Use the hierarchical Laplace fit to initialise PGAS?

todo:
exploratory hierarchical Laplace SSVS;
vectorized GEV particle and path likelihoods;
grouped xi progress output;
clearer changed_fraction naming;
optional parallel channel updates;
an HPC four-independent-chain example.